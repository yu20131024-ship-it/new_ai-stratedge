
## v2.3.44 Agnes 連線修復

Agnes AI 現在改由 `/.netlify/functions/agnes-proxy` 同源中介呼叫，瀏覽器不再直接 POST 到 `apihub.agnes-ai.com`，因此不再把瀏覽器 CORS 當成正常 API 失敗。Proxy 只允許 Agnes 官方 API 網域：`apihub.agnes-ai.com`、`apihub.agnes-ai.cn`、`api.agnes-ai.cn`，且不記錄 API Key。

若部署後仍出現 `502 upstream_network_error`，代表是 Netlify 執行環境到 Agnes API 的網路/DNS 路由問題，而不是瀏覽器 CORS；可將 `AGNES_BASE_URL` 設為可連線的 Agnes 官方端點。
# 策引 AI — 部署與設定說明（Google 登入 + 管理後台）

**目前版本：v2.3.43**（前端 Google 登入關卡暫時停用；Agnes 加入 14 RPM／29 RPD／9,999 TPM 的本地配額保護）

> **目前測試狀態（暫時）**：前端 Google 帳號登入關卡已停用，開啟網站即可直接使用；Netlify Identity 與管理後台的伺服器端權限驗證仍保留。測試完成後，將 `window.AUTH_GATE_DISABLED_FOR_TESTING` 改回 `false` 即可恢復登入關卡。

> 📋 **正在進行 12 階段程式改善計畫**，目前進度、診斷內容、每階段品管檢查紀錄與接手規則，
> 全部記錄在 [`_project-improvement/PROGRESS.md`](./_project-improvement/PROGRESS.md)，
> 任何人或 AI 要接續改善工作前，請先閱讀該檔案。

- **v2.3.43**：安全與配額防護強化。① 正式版重新啟用登入關卡；② Agnes API 加入嚴格的 14 RPM、29 RPD、9,999 TPM 本地守門，所有 retry 也必須先取得配額，超量時等待額度釋放或改用規則模擬，不採取繞過供應商限制的方式；③ 管理員 email 改由 Netlify `ADMIN_EMAIL` 環境變數控制，未設定時管理權限預設關閉；④ 加入基本安全回應標頭與下載 API `no-store`；⑤ 新增配額自動化測試。
## 版本紀錄
- **v2.3.41-testing**：修正使用者回報的視覺 bug——歸因模擬頁籤長條圖下方空白過大。根因：歸因模擬用的是純 SVG、依管道數量而定的矮圖表，卻跟主要分析頁的 Chart.js canvas 大圖表共用同一個 `chart-box` class 名稱，樣式表裡有一條規則把所有 `chart-box` 都強制設成 34rem（約544px）固定高度（是專門給 canvas 大圖表用的），歸因模擬的小圖表因此被硬撐開。修法：圖表容器多加一個區分用的 class，用組合選擇器把高度改回依內容自動撐高，不影響其他 3 個仍需要固定高度的 Chart.js 大圖表。新增 4 項測試。累計 15 組、179 項斷言全數通過。
- **v2.3.40-testing**：階段5 低風險子項——locked 流程步驟改為「可點＋懸停提示」。路線圖要求解決「按鈕沒反應」的觀感問題。追查後發現點擊 locked 步驟其實本來就會顯示 toast 說明還缺什麼——真正的問題是兩處純視覺呈現：①JS 用 `!important` 把游標鎖死成 `not-allowed`；②CSS 用蓋滿整顆按鈕的不透明遮罩蓋住文字。兩者加在一起讓使用者誤以為按鈕死掉了，因此不會去點，看不到那則其實已經寫好的說明。修正：游標改為 `pointer`，遮罩改為右上角鎖頭小徽章（不擋點擊，按鈕文字清楚可讀），並新增滑鼠懸停即顯示說明文字（不必等點擊）。判斷邏輯本身完全未被更動，只修正了視覺呈現。新增 10 項測試。累計 14 組、175 項斷言全數通過。
- **v2.3.39-testing**：階段4「頁籤整併」（低風險版本）。使用者已在瀏覽器實測確認前幾版所有頁面正確無誤，開始處理需要 DOM 結構調整的階段4。把輸出區 10 個扁平頁籤整併為 3 個語意分區——**策略洞察**（數據視圖／基準資料／歸因模擬／CFO級ROI引擎）、**執行產出**（文案版本／實驗計畫／素材指令／策略指令庫／業務開發名單）、**治理**（法規安全檢核）。刻意採用風險最低的做法：完全不改動 `switchOutTab()` 的顯示切換機制、不動任何頁籤內容、不搬動任何按鈕的 onclick／aria 屬性，只在既有按鈕外層包上 3 個帶標題的視覺分組容器，3 組全部展開顯示，使用者到任一內容仍只需 1 次點擊。新增 CSS 全部沿用既有語意變數，未新增硬編色碼。新增 17 項測試逐一核對按鈕目標未貼錯、分區數量符合規劃、原有切換機制未被更動。累計 165 項斷言全數通過。
- **v2.3.38-testing**：階段6全面完成 ＋ 階段7緊鄰補丁合併。①**階段6**：剩餘 32 處 `#FF0000`／`#8B1E1E`／`#A01818` 視覺統一替換已套用，連同先前的 18 處 `#DC2626`，4 種危險紅全數收斂為 `var(--danger)`。②**階段7**：合併「真正緊鄰、標籤間完全沒有夾雜其他內容」的 style／script 補丁，script 37→26 個標籤；style 的真實標籤數（用修正後的量測工具重新量測）是 57→22 個。③**過程中連續抓到並修正四個「靜態比對表面上會過關、其實內容被誤判」的陷阱**（合併工具會把落在 script 字串內、HTML 註解內的字面樣式標籤文字誤判成真標籤；會把帶有 src/onerror 的外部腳本標籤誤當成可合併補丁而蓋掉其屬性；發現本專案長年沿用的「79／80個 style 標籤」基準本身就是用天真正則量測出的虛高數字）。所有合併都以「合併前逐字記錄的原始片段」做精確比對驗證後才套用；本專案內建的邊界檢查與語法檢查工具也一併升級為同一套 comment-aware tokenizer。新增 9 項測試。累計 148 項斷言全數通過。
- **v2.3.37-testing**：回應使用者提問「Agnes Key 會不會誤打到 Claude？」「是不是超過 RPM 20？」。①**確認不會誤打**：`callAI()` 的分派邏輯（`provider==='agnes'` 時直接呼叫 `callAgnes` 並 return，寫在 `callClaude` 分支之前）程式邏輯上不可能執行到 Claude，已用真實原始碼端對端測試證明 claude/gemini 呼叫次數皆為 0。②**新增 RPM 觀測與軟性節流**：`AgnesRateTracker` 記錄過去 60 秒內每一次真正送出的請求（含重試，因為重試同樣算入供應商的 RPM 配額），這個數字現在會顯示在診斷面板與除錯資訊複製功能中，下次遇到 429 時畫面上就能直接看到「過去60秒送出了幾次」；當接近警戒線（18，低於常見的 RPM 20）時，新請求會主動延後送出以降低自行觸發限流的機率。新增 12 項測試。
- **v2.3.35**：階段6「危險色收斂」——路線圖點名的範例 #DC2626／#FF0000／#8B1E1E／#A01818 共 4 種紅。**已套用**其中 `#DC2626 → var(--danger)` 共 18 處：因為 `--danger` 的定義值本身就是 `#DC2626`，替換後瀏覽器算出來的顏色逐位元相同，是單純去重複而非視覺變更，可以安全直接執行。**其餘 32 處**（`#FF0000`／`#8B1E1E`／`#A01818`）若收斂會讓顯示顏色真的改變，屬於刻意的視覺統一，依專案鐵律未經真實瀏覽器截圖比對不應執行——新增 `tools/plan-color-consolidation.js` 乾跑規劃工具，產出每一處的行號與 CSS 上下文，供有瀏覽器的接手者審閱後用 `--apply` 一鍵套用（輸出到獨立檔案，不覆蓋原檔）。掃描範圍明確排除報告匯出範本（那是完全獨立的另一份文件、有自己的配色系統）與 token 定義行本身。新增 5 項測試驗證值保留正確性。
- **v2.3.34**：階段8「報告合併輸出管線」。①**完整分析結果快取**：分析成功後把最終結果整份存進 IndexedDB（沿用階段2的暫存層），同一份輸入＋供應商＋模式，即使重新整理頁面也不再重打 API——不只 Agnes 的分段路徑，Claude／Gemini 的單次分析也一併受益，這是路線圖「產生報告零 token」的完整實作。②**單章重新產生**：`regenerateAnalysisSegment()` 只清掉指定段落的暫存並只重打那一段，其餘段落直接沿用；未分段的供應商呼叫時會明確拋錯而非靜默整份重跑。③**修正 PDF 下載的真實 bug**：彈出視窗被封鎖時，舊版直接警告並丟棄整份已產生的報告內容，使用者只能從頭再跑一次；改為引導使用者手動點擊按鈕開啟（真實使用者手勢不受封鎖影響），內容不再遺失。④報告的列印樣式補上完整分頁規則（標題不孤立、卡片與表格不被硬切、跨頁重複表頭）。新增 21 項測試，並修正測試工具本身的一個註解解析 bug（中文註解裡的字面 `}` 字元原本會被誤算進大括號深度）。
- **v2.3.33**：階段2「分段生成＋逐段落地暫存」——**429 與逾時的治本修法**。前兩版縮小的是輸入端的 system prompt，但真正的大頭在輸出端：單次請求的 `max_tokens` 開到 13,000，請求越大就越容易被第三方供應商判定為異常大流量而限流、生成時間越長越容易逾時，而且**一旦被截斷或中途失敗，整份分析全部重跑**。本版把單次大請求拆成兩段（競品與 SWOT 5,000 tokens ＋ 五層分析 5,000–8,000 tokens），`buildSystemPrompt()` 新增 `scope` 參數讓每段只帶自己用得到的規則（五層分析段不再夾帶競品查核、來源查證與只為查競品而存在的權威來源清單），每段完成立即寫入 IndexedDB；**中途失敗或關掉頁面後重跑，已完成的段落直接讀暫存、只重跑失敗的那一段**。向後相容：預設只對 Agnes 啟用，Claude／Gemini 維持原本單次路徑完全不動，且已驗證 `buildSystemPrompt()` 不傳 `scope` 時輸出與 v2.3.32 逐字相同；其他供應商可用 `window.SEGMENTED_ANALYSIS = true` 手動開啟。IndexedDB 不可用（隱私模式）時分析仍正常完成，只是沒有續傳能力。新增 29 項端對端測試（假 AI ＋ 假 IndexedDB）。
- **v2.3.32**：接續 v2.3.31，處理 429 的真正成因之一。①**重試退避改為「速率限制感知」**——舊版不論 429 還是 503，一律套用「1.5 秒 → 3 秒 → 6 秒」這套給伺服器瞬間忙線用的節奏，而且完全忽略回應中的 `Retry-After` 標頭；429 的語意是「這段時間內用量已超過配額」，1.5 秒後再打只會再吃一次 429，**連打三次等同於在被限流的當下持續敲門，正是第三方服務把帳號暫時停用的典型觸發條件**。修法：支援 `Retry-After`（秒數與 HTTP 日期兩種格式）、429 等待下限拉高到 8 秒起跳並指數成長、加入隨機抖動避免多用戶端同步重試；伺服器要求等待超過 60 秒則停止重試並顯示明確錯誤，不讓畫面卡住。新增 19 項自動化測試覆蓋。②**階段6 前置盤點**：新增 `tools/color-inventory.js` 與 `stage6-color-inventory.md`，569 種色碼可收斂為 12 個語意分組（路線圖目標 ≤20，可達成）。盤點過程另發現既有無障礙問題：`--danger` (#DC2626) 在深色底上對比度僅 4.22:1，達不到一般文字的 4.5:1，已記錄待修（屬既有問題，非本次改動造成）。色碼替換本身**刻意未執行**，理由見 PROGRESS.md。
- **v2.3.31**：依《strat-edge 10 階段執行路線圖》執行階段 1／2／3／7（部分）。①**27 個各自獨立的 `DOMContentLoaded` 初始化區塊整併為單一佇列 `SEInit`**——舊架構下任一區塊丟出例外，後面的初始化就整批不執行且無從查起；新架構每步驟各自 try/catch、失敗只影響該步驟並記錄到 `SEInit.errors`。特別保留 phase 0／1 以完整重現瀏覽器原生的「document 監聽器早於 window 監聽器」派送順序，確保初始化時機與改版前逐一等價。②**System Prompt 改為依使用者勾選的投放管道與產業別動態組裝**：產業基準數據庫與十大類權威來源清單拆成帶條件的結構化資料，只送出本次用得到的段落，實測 14,291 → 11,951 字元（-16.4%），所有原始數字逐字保留並由測試斷言無遺失。③**歸因模擬改為可重現的蒙地卡羅**：`Math.random()` 改為以輸入為種子的 `SimRandom`，同一組管道重算必得逐位元一致的結果，模擬次數提高到 10,000 次；新增 `DATA_SOURCE` 來源抽象層與標籤元件，歸因面板已掛上「模擬推估」標示並說明非真實投放數據，另預留 `live-api` 分類供未來串接 GA4／Meta Ads。④新增**零依賴**的功能凍結護欄與 4 支自動化測試（`node _project-improvement/tests/run-all.js` 一鍵執行），並實測「故意刪一顆按鈕會轉紅」。**已知限制：本次執行環境無網路與瀏覽器，真實瀏覽器視覺回歸與對比度量測未執行，建議 push 前在有瀏覽器的環境補跑一次。** 路線圖的階段 4／5／6 與輸出端分段生成刻意未執行，理由詳見 PROGRESS.md 第 11 節。
- **v2.3.30**：12階段改善計畫「階段6：整併`:root`配色核心」。原本144個CSS變數分散定義在主樣式表＋4個「全站主題重做」補丁共10個`:root`區塊裡，現在統一成主樣式表裡單一一個乾淨的`:root`區塊；其餘4個補丁只移除`:root`本身，其他624條非`:root`規則原樣保留、位置不變。這是階段4發現的「配色不一致真正結構性根源」的正式修復——不是改變任何一個變數的值，而是讓「這個變數現在到底是多少」只需要看一個地方就好。驗證（比照階段5的四層驗證流程）：144個CSS變數整併前後最終生效值逐一核對完全相同；2406條非`:root`規則的選擇器/宣告/順序逐項比對完全相同；標籤邊界與全域命名碰撞檢查正常；真實瀏覽器視覺回歸截圖比對，差異在已知雜訊基準值內。
- **v2.3.29**：12階段改善計畫「階段5：合併第一批小型style補丁（結構合併部分）」。把66個「真正逐位元組緊鄰、不碰:root」的連續小型補丁合併成11個`<style>`區塊，CSS規則內容與順序完全不變，`!important`移除留給後續階段。**過程中發生兩次事故並已修正，如實記錄**：①第一版「相鄰」判斷方式有誤，把中間夾著一整段匯出報告樣板字面值的兩個補丁誤判為相鄰，合併時把中間內容整段吞掉導致頁面壞掉；②修正後的說明註解不慎寫了字面上的閉合標籤文字，導致該`<style>`標籤被瀏覽器提前截斷（CSS規則總量剛好不變，只看規則內容比對看不出問題）。兩次都已修正，完整教訓記錄於 `_project-improvement/PROGRESS.md`。發現此環境已預裝Playwright headless瀏覽器，新增 `_project-improvement/tools/visual-regression-check.py` 做真實截圖像素比對，連同CSS規則AST比對、標籤邊界驗證（新增`verify-style-boundaries.js`）、全域碰撞檢查共四層驗證，確認畫面與改動前一致。
- **v2.3.28**：12階段改善計畫「階段4：盤點style/script補丁，產出合併分類清單」。本版**只新增文件與分析工具，未修改任何會被執行的程式碼**（index.html除版本號外逐位元組不變，後端function完全未變動）。重要更正：階段1的「130個style補丁」實際為111個在本頁面、19個位於匯出報告樣板內。新發現：CSS設計變數(`:root`)被重複定義10次，分散在主樣式表與4個「全站主題重做」補丁裡，144個曾定義的變數目前生效值來自5個不同地方——這是配色不一致的結構性根源，完整分析見 `_project-improvement/stage4-patches-inventory.md`。新增可重複執行的分析工具於 `_project-improvement/tools/`。過程中發現原用的 `css` 套件有中等風險安全性警告（僅影響開發工具、不影響部署），已主動換成無警告的 `postcss`／`postcss-selector-parser` 並重新驗證數字一致。
- **v2.3.27**：12階段改善計畫「階段3：清除重複函式定義與其他重複程式碼」。**診斷修正**：階段1原本以為 `esc()` 定義兩次是bug，深入查證後發現第二處其實是「產生獨立可下載互動報告」功能組出的 HTML 字串內容，是那份匯出後獨立運作的檔案自己需要的，跟 index.html 是兩個不同的全域作用域，互不影響——已用 AST 語法樹分析（acorn）＋符合 HTML5 規範的解析器（parse5）交叉驗證後**維持原樣未刪除**（真的刪掉會讓每份下載的互動報告在使用者電腦上打開時報錯）。真正找到並修正的重複：管理後台渲染邏輯裡多了一個內容跟 `esc()` 逐字元相同的 `esc21()`，已移除、改呼叫共用的 `esc()`。新增 `_project-improvement/tests/stage3-admin-panel-render.test.js`（需要 jsdom，已加入 devDependencies，不影響部署），直接抽取真實原始碼在模擬瀏覽器環境驗證：用含 XSS 測試字串的假資料確認轉義行為正確，並比對改動前後渲染輸出逐字元 100% 一致。
- **v2.3.26**：12階段改善計畫「階段2：修正後端併發寫入競態條件與效能問題」。`log-event.js`／`delete-download.js` 原本對共用的 `all-events` 紀錄陣列採「整包讀出→修改→整包寫回」且無任何保護，兩個請求幾乎同時發生時（例如使用者下載報告與管理者刪檔同時發生）會互相覆蓋、悄悄遺失對方剛寫入的紀錄。修法：升級 `@netlify/blobs` 至 10.7.13 取得 `onlyIfMatch`/`onlyIfNew` 條件式寫入能力，新增共用函式 `updateLogsWithRetry()` 實作「樂觀鎖＋指數退避重試」，兩支 function 皆改用此函式；純後端修正，前端 `index.html` 無任何邏輯或樣式變更。新增可重複執行的自動化併發測試 `_project-improvement/tests/stage2-concurrency.test.js`，直接驅動真實後端程式碼驗證（50/150筆併發寫入不遺失、下載與刪除同時發生兩邊皆保留、既有錯誤處理與5000筆上限規則不受影響、重試耗盡時正確回報而非靜默失敗），已知限制：本開發環境無法連線真實 Netlify Blobs 服務做端對端測試，建議正式上線前於 Deploy Preview 實測登入/下載/刪除三個操作一次。
- **v2.3.25**：全面程式體檢後啟動之 12 階段改善計畫，本版對應「階段1：基礎建設」。新增 `_project-improvement/PROGRESS.md`（完整診斷摘要、12階段計畫表、每階段強制品管檢查規則、接手鐵律）與 `_project-improvement/backups/stage-0-original/`（改善前 v2.3.24 版 index.html 逐位元組備份＋SHA-256 校驗碼）。**本版未變更任何既有功能邏輯與畫面樣式**，僅版本號本身、`index.html` 內對應的版本歷史註解、與新增之專案管理文件為本次唯一異動；已用校驗碼驗證除版本字串外核心程式碼與 v2.3.24 完全一致。品管檢查：本版無新增互動元件、無變更任何顏色樣式，故按鈕觸發與文字/背景對比度與 v2.3.24 相同，無需重測；已確認頁面標題、右上角版本徽章、`window.APP_VERSION` 三處版本號顯示一致，且徽章文字對比度沿用既有樣式未受影響。
- **v2.3.24**：需求：不管使用者有沒有登入（包含用瀏覽器開發者工具繞過前端 Google 登入關卡的情況），只要點擊下載按鈕產生報告，管理後台都要留一份備份。修法：① 後端 `log-event.js` 的 `download` 事件改成不管有沒有登入都放行（`login` 事件維持原本一定要登入才能記錄）；沒有登入身份時，記錄會標記 `isAnonymous:true`、姓名顯示為「（未登入使用者）」，IP／瀏覽器／檔案內容仍完整保留，並加上 6MB 的匿名檔案大小上限避免這支開放的 API 被濫用塞爆儲存空間。② 前端 `__logEvent()` 不再因為沒有登入憑證就放棄記錄，下載事件一律送出，沒有 token 時改用不帶身份的方式呼叫。③ 管理後台面板新增「未登入下載」紅色警示區塊，獨立列出這些無法歸戶到特定帳號的下載紀錄（時間／IP／瀏覽器／檔案下載連結），不會誤混進其他登入使用者的分組清單。已知限制：如果使用者連呼叫記錄用的那行程式碼本身都手動移除再下載，這種刻意規避追蹤的情況仍然抓不到，純前端記錄機制無法根治，需要把產生報告的核心邏輯搬到後端才能徹底解決，不在本次修改範圍。已用自動化測試腳本驗證後端 4 支 function（含匿名下載、大小上限、管理者查詢與重新下載、非管理者被擋）與前端管理後台渲染邏輯（含混合資料、全部匿名、完全無資料三種情境），全數通過。
- **v2.3.20**：修復管理後台「查詢失敗（狀態碼 401）」。根本原因：前端四個呼叫後端 function 的地方（記錄登入/下載事件、載入管理後台紀錄、刪除檔案、重新下載檔案）都是直接讀取登入時拿到的 `user.token.access_token` 當作 Authorization Bearer token，但 Netlify Identity 核發的 access token 效期只有約 1 小時，分頁開著久一點、或距離上次登入有一段時間，token 就已經過期；後端驗證過期 token 會判定為未登入（`context.clientContext.user` 是 undefined），對應到 `admin-logs.js` 就是回傳 401（`not_authenticated`）。修法：四個呼叫點都改成先 `await user.jwt()` 再發送請求——這是 netlify-identity-widget 官方建議的正確用法，會自動判斷目前 token 是否已過期，過期的話用登入時一併取得、效期長很多的 refresh token 在背景換一顆新的有效 token，沒過期則直接沿用，不會每次都多打一次網路。
- **v2.3.19**：① 修復管理後台「查詢失敗（狀態碼 502）」——`admin-logs`/`log-event`/`get-download`/`delete-download` 四支 function 用的是舊式（Lambda 相容模式）handler 寫法以讀取 Netlify Identity 資訊，但 Netlify Blobs 的 `getStore()` 在這種模式下不會自動讀到執行環境設定，必須先呼叫 `connectLambda(event)`，否則會丟出未被接住的 `MissingBlobsEnvironmentError`、被 Netlify 直接判定為 502。已在 `_utils.js` 集中修正並加上 try/catch。② 修復右上角標語文字和「API 設定／🔒 管理後台／登出」按鈕重疊、字疊字看不清楚——原因是標語文字用固定 50% 置中、未考慮按鈕群實際寬度，加上兩段舊實驗主題殘留的 `::after` 除錯文字（"Backend-ready" / "Proxy-ready"）疊加顯示。已移除殘留文字，並將標語定位改為由 JS 即時量測可用空間動態計算，避免任何視窗寬度或按鈕數量下再度重疊。
- **v2.3.18**：修復以 felix670131@gmail.com 登入後，右上角「🔒 管理後台」按鈕沒有出現的問題。根本原因是 netlifyIdentity 的 'init' 事件在偵測到瀏覽器已有先前登入紀錄時，會幾乎立刻同步觸發，快到判斷程式執行當下，瀏覽器還沒讀取到後面 HTML 裡的管理後台按鈕元素，導致抓取失敗、按鈕永遠不顯示。已修正為在整個頁面載入完成後再檢查一次登入狀態；同時信箱比對加上 `.trim()` 防止空白字元誤判（前端與後端皆已同步修正）。**這與 Netlify 環境變數無關**——管理員身分驗證原本就不需要額外設定任何環境變數 Key，只認登入者的 Google 帳號是否等於程式內寫死的管理者信箱。
- **v2.3.17**：僅補上專案文件，無程式碼功能變更。新增 `docs/AI協作除錯指南.md`，記錄 v2.3.16 Google 登入疑難排解的完整經過與根本原因，並整理成「非IT/非工程背景使用者如何委任 AI 直接處理問題」的溝通指南與可複製開場白範本。
- **v2.3.16**：修復「使用 Google 帳號登入」按鈕點擊沒反應的問題。根本原因是 Netlify Identity widget 彈出的登入小視窗有官方已知的 z-index 疊層臭蟲（github.com/netlify/netlify-identity-widget/issues/94、#67），本頁全螢幕登入遮罩的 z-index 比登入小視窗還高，導致小視窗雖然有正常開啟，卻被整個蓋住看不到。已強制登入視窗 z-index 為最大值，並新增「改用直接跳轉登入」備用連結。
- **v2.3.15**：管理後台新增「刪除檔案」功能。管理者可在下載紀錄旁點擊「🗑 刪除」，移除該筆下載實際儲存的檔案內容；刪除後紀錄仍保留（登入者、時間、檔名），只有檔案本體變成「無檔案」並標註刪除者與刪除時間。
- **v2.3.14 以前**（即前一次交付）：Google 帳號登入關卡、管理後台（登入紀錄／IP／瀏覽器／下載紀錄／重新下載檔案）。

這個資料夾是一個完整的 Netlify 專案，包含：
- `index.html` — 主應用程式（已加入 Google 登入關卡與管理後台入口）
- `netlify/functions/` — 3 支伺服器端函式，負責記錄登入/下載事件、管理者查詢
- `netlify.toml` — Netlify 建置設定
- `package.json` — 宣告 `@netlify/blobs` 依賴套件

## 一、把專案放上 GitHub

```bash
cd strat-edge-app
git init
git add .
git commit -m "初始版本：Google 登入 + 管理後台"
```

接著到 GitHub 建立一個新的（可設為 Private）repository，然後：

```bash
git remote add origin https://github.com/你的帳號/你的repo名稱.git
git branch -M main
git push -u origin main
```

## 二、連接 Netlify

1. 登入 [app.netlify.com](https://app.netlify.com)
2. 點「Add new site」→「Import an existing project」→ 選擇剛剛的 GitHub repo
3. Build command 留空、Publish directory 填 `.`（因為是純靜態網站＋functions，不需要建置指令）
4. 點「Deploy site」

## 三、啟用 Netlify Identity（Google 登入）— 重點步驟

1. 進入你的 Netlify 網站後台 → **Project configuration**（或 Site configuration）→ **Identity**
2. 點 **Enable Identity**
3. 往下找到 **Registration** 設定：
   - 建議把 Registration 設為 **Invite only**（僅限邀請）——這樣才不會有陌生人自己註冊帳號進來用你的分析工具。之後你要讓誰使用，就到 Identity 分頁手動「Invite user」輸入對方 email。
4. 找到 **External providers**（外部登入提供者）：
   - 打開 **Google** 這個選項即可，**不需要**自己申請 Google Cloud OAuth Client ID（Netlify 會用它自己的共用 Google 應用程式幫你處理，使用者在 Google 同意畫面上會看到「Netlify Identity」字樣，這不影響功能）。
   - 如果你之後想讓使用者在 Google 登入畫面看到「策引 AI」而不是「Netlify Identity」，才需要自己申請 Google Cloud OAuth Client ID 並填入這裡的欄位，這是選配，不是必要條件。

完成後，重新整理你的網站，應該就會看到登入關卡畫面，按「使用 Google 帳號登入」即可測試。

## 四、設定管理員環境變數

在 Netlify Site configuration → Environment variables 設定：

- `ADMIN_EMAIL`：唯一可查看管理後台的管理員 email。
- 未設定 `ADMIN_EMAIL` 時，管理後台權限會全部關閉。

## 四、確認 Blobs 功能已生效

`@netlify/blobs` 是 Netlify 平台內建功能，只要網站是透過 Netlify 部署，`netlify/functions/` 裡的程式碼呼叫 `getStore(...)` 就會自動生效，**不需要**額外申請帳號或設定金鑰。

## 五、管理後台怎麼用

1. 用已設定於 Netlify `ADMIN_EMAIL` 的 Google 帳號登入網站（記得先在 Identity 分頁邀請該 email，否則連登入都進不來）
2. 登入後，右上角會出現一個「🔒 管理後台」按鈕（其他帳號登入不會看到這個按鈕）
3. 點開後可以看到：
   - 每個使用者的登入次數、最後登入時間
   - 展開後可看到每一筆登入的 IP 位址、瀏覽器資訊
   - 每一筆下載紀錄（下載了什麼報告、什麼時候、什麼格式），並可以直接點「下載」重新取得那份檔案

這份資料存在 Netlify Blobs（伺服器端），不管你、或任何登入者換了電腦、換了瀏覽器，這些紀錄都還在，不會因為清瀏覽器快取或換裝置就消失。

## 六、之後要修改程式碼

以後不論是我這邊繼續幫你加功能，或是你自己想調整，流程都是：
1. 修改 `index.html`（或 `netlify/functions/` 裡的程式）
2. `git add . && git commit -m "說明這次改了什麼" && git push`
3. Netlify 偵測到 GitHub 有新的 commit，會自動重新部署，通常 1-2 分鐘內就會上線新版本

## 七、需要注意的限制（誠實說明）

- Netlify Identity 是給「一般網站的訪客登入」使用，如果未來使用者規模變大（例如超過幾千人），會建議换成更完整的身份驗證服務（例如 Auth0），但目前規模下完全夠用。
- Netlify Blobs 沒有像正式資料庫一樣的複雜查詢功能（不能下 SQL 篩選），這個 MVP 版本是把全部紀錄整包讀出來、在瀏覽器端做分組顯示，使用者與下載紀錄數量在幾千筆以內都不會有效能問題；如果之後用量大幅成長，會建議改用正式資料庫（例如 Supabase）。
- 瀏覽器端 JavaScript 本質上都可以被使用者用「檢視原始碼」看到，但這不影響安全性：真正決定「誰能看管理後台資料」的判斷完全在 `netlify/functions/admin-logs.js` 這支伺服器端程式碼裡，前端只是負責要不要「顯示按鈕」的體驗優化。
